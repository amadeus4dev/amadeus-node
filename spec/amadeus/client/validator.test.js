import {initRequired, initOptions} from '../../../src/amadeus/client/validator';

describe('Validator', () => {
  test('exports the functions', () => {
    expect(initRequired).not.toBe(null);
    expect(initOptions).not.toBe(null);
  });

  describe('.initRequired', () => {

  });

  describe('.initOptions', () => {

  });
});
